using Verse;

namespace StellarisDaughter
{
    // ✨ 沐雪写的哦~
    /// <summary>
    /// 语音事件类型标识 — 纯 Def，不带 Worker。
    /// 事件的触发时机由各 Harmony Patch 负责，Def 只是分类 key。
    /// 通过 XML 新增 defName 即可定义新的事件类型。
    /// </summary>
    public class VoiceEventDef : Def
    {
    }
}
